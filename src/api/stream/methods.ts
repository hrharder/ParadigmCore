/*
import * as ws from "ws";
import { StreamServer } from "./StreamServer";

export const methods = {
    "session.end": (server: StreamServer, client: ws.WebSocket, params: any) => {
        client.close();
    }
}
*/